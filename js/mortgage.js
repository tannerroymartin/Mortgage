//when user clicks the button
buildPaymentSchedule() {
    //get some values
    paymentArray = getPayments();
    displayData(paymentArray)
}

//build an array of payments
getpayments(){
//build your amorization schedule, fizzbuzz for other  stuff 
}

displayData(paymentArray)
{

}